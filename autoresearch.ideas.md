- Validate generated Go default `GOGC=300` against peak RSS and longer-running/memory-sensitive workloads; users can override with `GOGC`. Quick decode_pipeline spot check showed default target used ~27 MB RSS vs `GOGC=100` ~20 MB RSS. GC targets 100/1000/off were rechecked after strict lazy JSON and regressed.
- Review semantics of `DecodeIntListErrorsExtern` typed `[]int` fast path and validation-time typed collection caches before finalizing; current speed wins can return/cache original typed slices/maps. Copying and cap-limiting regressed, so this is a correctness/API decision rather than an obvious perf follow-up.
- Expand strict lazy JSON validation tests before final review: compare against `ffi.JsonToDynamic`/json-v2 for valid/invalid numbers, literals, escaped strings, unicode escapes/surrogates, invalid UTF-8 if applicable, trailing separators, missing commas, nested duplicate keys in arrays, typed-cache fallback for non-int collections, string-int maps above the eight-key typed-cache limit, and generic fallback for unsupported nested shapes. Leading-zero arrays/maps and sign-only negatives are now covered.
- Validate lazy JSON object cache thresholds on broader JSON workloads. Three cached fields with pointer/interface storage is fastest for decode_pipeline but may be too narrow; four/eight-slot variants are more general but slower here.
- If revisiting decode field closure removal, target generated code shape directly; runtime helper lowerings and stdlib named-function returns both regressed/failed checks.
- Try to recover the remaining strict typed integer validation cost without weakening correctness; split slow parsing, inlined positive delimiter switch, and negative digit lookahead helped, but noinline/cached-first-byte/post-scan validation/split negative helper regressed.
