- Validate generated Go default `GOGC=300` against peak RSS and longer-running/memory-sensitive workloads; users can override with `GOGC`. Quick decode_pipeline spot check showed default target used ~27 MB RSS vs `GOGC=100` ~20 MB RSS.
- Review semantics of `DecodeIntListErrorsExtern` typed `[]int` fast path before finalizing; current speed win returns the original slice. Copying and cap-limiting were tried and regressed, so this is a correctness/API decision rather than an obvious perf follow-up.
- Expand strict lazy JSON validation tests before final review: compare against `ffi.JsonToDynamic`/json-v2 for valid/invalid numbers, literals, escaped strings, unicode escapes/surrogates, invalid UTF-8 if applicable, trailing separators, missing commas, nested duplicate keys in arrays, and fallback for unsupported nested shapes.
- Validate lazy JSON object cache thresholds on broader JSON workloads. Three cached fields with pointer storage is fastest for decode_pipeline but may be too narrow; four/eight-slot variants are more general but slower here.
- If revisiting typed collection caching, integrate int-list/string-int-map parsing into the validation scan rather than eagerly rescanning cached field substrings; the eager rescan cache regressed.
