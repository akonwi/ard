use ard/io

mut numbers: [Num] = []
io.print("numbers.size = {{numbers.size}}")

io.print("adding numbers from 0 to 10")
for i in 0..10 {
  numbers.push(i)
}

io.print("numbers.size = {{numbers.size}}")
for n in numbers {
  io.print(n.as_str)
}
