use ard/io
use ard/maybe

// a declared none value needs to be explicitly typed
mut name: Str? = maybe.none()

// update the value
name = maybe.some("Akonwi")

// read the value
match name {
  n => io.print("name is: {{n}}"),
  _ => io.print("no name")
}
