use std/io

fn fib(n: Num) Num {
  mut result = n
  if n > 1 {
    result = fib(n - 2) + fib(n - 1)
  }
  result
}

for n in 20 {
  let result = fib(n)
  io.print("fib({{n.as_str}}) = {{result.as_str}}")
}
