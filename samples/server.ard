use ard/http

fn main() {
  http::serve(3000, [
    "/": fn(req: http::Request) http::Response {
      http::Response::new(200, "Hello, World!")
    },
    "/me": fn(req: http::Request) http::Response {
      http::Response::new(200, "this is {req.path().expect("Invalid path")}")
    },
  ])
}
