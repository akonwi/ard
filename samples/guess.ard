use ard/io

io::print("Guess the number!\nPlease input your guess:")
match io::read_line() {
  ok(guess) => io::print("You guessed: {guess.trim()}"),
  err => io::print("Failed to read line: {err}")
}
