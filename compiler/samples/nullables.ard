use ard/maybe

use go:fmt

fn main() {
  // a declared none value needs to be explicitly typed
  mut name: Str? = maybe::none()

  // update the value
  name = maybe::some("Akonwi")

  // read the value
  match name {
    n => fmt::Println("name is: {n}"),
    _ => fmt::Println("no name")
  }
}
