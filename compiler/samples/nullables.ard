use go:fmt

fn main() {
  // a declared none value needs to be explicitly typed
  mut name: Str? = Maybe::new<Str>()

  // update the value
  name = Maybe::new("Akonwi")

  // read the value
  match name {
    n => fmt::Println("name is: {n}"),
    _ => fmt::Println("no name"),
  }
}
