use ard/async
use ard/channel
use ard/io

fn main() {
  let done = channel::new<Bool>(0)

  async::start(fn() {
    io::print("working in a goroutine")
    done.send(true)
  })

  // start is fire-and-forget; coordinate with a channel.
  let _ = done.recv()
  io::print("done")
}
