use ard/maybe

use go:github.com/akonwi/ard/std_lib/ffi

fn from_int(value: Int) Rune? {
  ffi::RuneFromInt(value)
}

fn from_str(value: Str) Rune? {
  let runes = value.runes()
  match runes.size() {
    1 => maybe::some(runes.at(0)),
    _ => maybe::none(),
  }
}
