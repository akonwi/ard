# Ard HTTP server

A small Ard HTTP server example using `ard/http` and `ard/decode`.

The server reads its port from `PORT` and falls back to `8000`.

## Run

```sh
PORT=8000 ard run main.ard
```

## Test

```sh
./test_server.py
```
